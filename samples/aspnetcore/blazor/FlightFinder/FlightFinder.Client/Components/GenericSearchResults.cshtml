

@functions
{
    // Private state
    //private int chosenSortOrder;
    //private IEnumerable<TResult> Sort(IEnumerable<TResult> items)
    //{
    //    if (SortOptions != null && chosenSortOrder < SortOptions.Count)
    //    {
    //        return items.OrderBy(i => i, SortOptions[chosenSortOrder].comparer);
    //    }

    //    return items;
    //}
}