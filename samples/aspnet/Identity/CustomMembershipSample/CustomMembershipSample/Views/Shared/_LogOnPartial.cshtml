@if(Request.IsAuthenticated) {
    <text>Welcome <strong>@User.Identity.Name</strong>!
    [ @Html.ActionLink("Log Off", "LogOff", "IdentityAccount") ]</text>
}
else {
    @:[ @Html.ActionLink("Log On", "Login", "IdentityAccount") ]
}

